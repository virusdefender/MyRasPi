#cmake 
# support connect to mysql 5.5 
#

cmake_minimum_required(version 2.8)

project(libTR)

option (USE_LOCALHOST
		"Use localhost as server" ON)

#test if we use localhost 

if (USE_LOCALHOST)
	
